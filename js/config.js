// Settings for THIS club's deployment. Edit these after forking, then
// commit/push — no other file needs to change to run your own copy.
window.TM_CONFIG = {
  // Paste the Web App URL you get from Apps Script "Deploy" here.
  // Example: "https://script.google.com/macros/s/AKfycb.../exec"
  WEB_APP_URL: "",

  // Shown in the page header.
  CLUB_NAME: "Your Toastmasters Club",

  // Optional. If set, edit.html asks for this before showing the editor.
  // This is NOT real security (it's visible in this file's source) — it's
  // just friction to stop casual/accidental edits. Real protection is
  // simply not sharing the edit.html link. Leave blank to disable.
  EDIT_PIN: ""
};
